image: node:14.20.1

stages:
  - build
  - deploy

cache:
  paths:
    - node_modules/

build:
  stage: build
  script:
    - pnpm install
    - pnpm build
  artifacts:
    paths:
      - dist/

pages:
  stage: deploy
  script:
    - mv dist public/
  artifacts:
    paths:
      - public/
  only:
    - master
